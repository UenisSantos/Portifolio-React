import styled, { css } from 'styled-components';

export const ButtomContainer = styled.button`
${({ menu, theme }) => css`

border:none ;
background-color:${menu ? 'transparent' : 'white'} ;
overflow:hidden ;














`}
`;
